# 012 — 走到指令畫面（M4 的前半）

日期：2026-09-06

```
189年 1月        命令者 曹操      兗州ー07国
                 君主：曹操       城の大きさ  4   人口     150000
                 太守：曹操       美女の数    0   民忠誠度     55
軍資金  1200     土地の価値 20    名馬の数   66   現役武将      7
兵糧   20000     洪水確率   88    鉄保有量    0   全兵士数  17100
借金       0                                      在野武将      0
金利     105     ｿｳｿｳ 様 御命令を (0-20)?
米相場   108
                 1.移動  2.争戦  3.輸送 … 20.その他
```

26 鍵的開新遊戲序列全程走通：空白 → Return → NEW GAME → 劇本 1 →
君主 1 → 人數 1 → 英雄 1 → 能力抽取（空白 ×5）→ Y → 電腦強度 5 →
性格 1 → Hex 顯示 2 → 最終確認 Y → 空白。

**8,000 萬道指令、9.7 億週期。** 用固定延遲那一版跑到同一個地方要 42 億道
指令而且會錯位（`docs/findings/011`）。

## 數字對得上文件

`軍資金 1200`、`土地の価値 20` 與 `sangokushi_x68k_cht` 的
`docs/playtest/03` 實驗 B 記的初始值一致（那份記的是「土地の価値 20 → 39、
軍資金 1200 → 0」的開發指令實驗）。這些值是從 `SAVEDATA` 的劇本記錄讀出來的。

## 路上補的最後一項

`IOCS $BC _PAINT`——遊戲要畫地圖時用的邊界填充。參數版面取自平台手冊
（x.w、y.w、調色盤碼.w、作業區起訖.l），語意是「從起點把所有不是那個顏色
的相連像素塗成那個顏色」。

**標成 L3，還沒對過 MAME。** 文字面已經 131,072 bytes 逐位元組比過
（`docs/findings/009`），圖形面一次都沒有——兩者不要混為一談。

## 還沒做

- 「下完一個月的指令」（M4 的後半）。
- 圖形平面對 MAME（M3 的後半）。
